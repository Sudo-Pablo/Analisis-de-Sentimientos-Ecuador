"""
Script de migración para actualizar la base de datos del sistema principal
para soportar múltiples plataformas (Facebook + TikTok).
"""
import os
import sys
from datetime import datetime

# Agregar la raíz del proyecto al path (subir 2 niveles desde scripts/development)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', '..'))

from src.database.db_manager import DatabaseManager
from src.utils.logger import get_logger
from sqlalchemy import text
import traceback

logger = get_logger(__name__)


class DatabaseMigrator:
    """Migrador para actualizar la estructura de la base de datos"""
    
    def __init__(self):
        """Inicializa el migrador de base de datos"""
        self.db_manager = DatabaseManager()
        self.migration_history = []
    
    def run_migration(self):
        """Ejecuta la migración completa"""
        logger.info("=== INICIANDO MIGRACIÓN DE BASE DE DATOS ===")
        
        try:
            # Crear backup de tablas actuales (opcional)
            self._create_backup()
            
            # Ejecutar migraciones
            self._migrate_posts_table()
            self._migrate_comments_table() 
            self._update_foreign_keys()
            self._add_indexes()
            self._verify_migration()
            
            logger.info("=== MIGRACIÓN COMPLETADA EXITOSAMENTE ===")
            self._print_migration_summary()
            
        except Exception as e:
            logger.error(f"Error durante la migración: {e}")
            logger.error(traceback.format_exc())
            self._rollback_migration()
            raise
    
    def _create_backup(self):
        """Crea backup de las tablas existentes"""
        logger.info("Creando backup de tablas existentes...")
        
        with self.db_manager.get_session() as session:
            try:
                # Backup de fb_posts
                session.execute(text("""
                    CREATE TABLE IF NOT EXISTS fb_posts_backup AS 
                    SELECT * FROM fb_posts
                """))
                
                # Backup de fb_comments
                session.execute(text("""
                    CREATE TABLE IF NOT EXISTS fb_comments_backup AS 
                    SELECT * FROM fb_comments
                """))
                
                session.commit()
                logger.info("Backup creado exitosamente")
                self.migration_history.append(\"Backup creado\")\n                \n            except Exception as e:\n                logger.warning(f\"Error creando backup: {e}\")\n                # No es crítico si falla el backup\n    \n    def _migrate_posts_table(self):\n        \"\"\"Migra la tabla de posts para soportar múltiples plataformas\"\"\"\n        logger.info(\"Migrando tabla de posts...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Renombrar tabla existente\n                session.execute(text(\"ALTER TABLE fb_posts RENAME TO social_posts\"))\n                logger.info(\"Tabla fb_posts renombrada a social_posts\")\n                \n                # Agregar nuevos campos\n                session.execute(text(\"\"\"\n                    ALTER TABLE social_posts \n                    ADD COLUMN IF NOT EXISTS platform VARCHAR(20) DEFAULT 'facebook',\n                    ADD COLUMN IF NOT EXISTS category VARCHAR(50),\n                    ADD COLUMN IF NOT EXISTS author_name VARCHAR(200),\n                    ADD COLUMN IF NOT EXISTS hashtags TEXT\n                \"\"\"))\n                logger.info(\"Nuevos campos agregados a social_posts\")\n                \n                # Hacer que page_id y topic_id sean opcionales (nullable)\n                session.execute(text(\"\"\"\n                    ALTER TABLE social_posts \n                    ALTER COLUMN page_id DROP NOT NULL,\n                    ALTER COLUMN topic_id DROP NOT NULL\n                \"\"\"))\n                logger.info(\"Campos page_id y topic_id ahora son opcionales\")\n                \n                # Actualizar registros existentes\n                session.execute(text(\"\"\"\n                    UPDATE social_posts \n                    SET platform = 'facebook' \n                    WHERE platform IS NULL\n                \"\"\"))\n                \n                session.commit()\n                self.migration_history.append(\"Tabla posts migrada\")\n                \n            except Exception as e:\n                session.rollback()\n                logger.error(f\"Error migrando tabla posts: {e}\")\n                raise\n    \n    def _migrate_comments_table(self):\n        \"\"\"Migra la tabla de comentarios para soportar múltiples plataformas\"\"\"\n        logger.info(\"Migrando tabla de comentarios...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Renombrar tabla existente\n                session.execute(text(\"ALTER TABLE fb_comments RENAME TO social_comments\"))\n                logger.info(\"Tabla fb_comments renombrada a social_comments\")\n                \n                # Agregar nuevos campos\n                session.execute(text(\"\"\"\n                    ALTER TABLE social_comments \n                    ADD COLUMN IF NOT EXISTS platform VARCHAR(20) DEFAULT 'facebook',\n                    ADD COLUMN IF NOT EXISTS category VARCHAR(50)\n                \"\"\"))\n                logger.info(\"Nuevos campos agregados a social_comments\")\n                \n                # Actualizar registros existentes\n                session.execute(text(\"\"\"\n                    UPDATE social_comments \n                    SET platform = 'facebook' \n                    WHERE platform IS NULL\n                \"\"\"))\n                \n                session.commit()\n                self.migration_history.append(\"Tabla comments migrada\")\n                \n            except Exception as e:\n                session.rollback()\n                logger.error(f\"Error migrando tabla comments: {e}\")\n                raise\n    \n    def _update_foreign_keys(self):\n        \"\"\"Actualiza las foreign keys para usar las nuevas tablas\"\"\"\n        logger.info(\"Actualizando foreign keys...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Actualizar foreign key de social_comments hacia social_posts\n                # Primero eliminar la constraint existente\n                session.execute(text(\"\"\"\n                    ALTER TABLE social_comments \n                    DROP CONSTRAINT IF EXISTS fb_comments_post_id_fkey\n                \"\"\"))\n                \n                # Agregar nueva constraint\n                session.execute(text(\"\"\"\n                    ALTER TABLE social_comments \n                    ADD CONSTRAINT social_comments_post_id_fkey \n                    FOREIGN KEY (post_id) REFERENCES social_posts(id) ON DELETE CASCADE\n                \"\"\"))\n                \n                # Actualizar foreign key de comment_sentiments\n                session.execute(text(\"\"\"\n                    ALTER TABLE comment_sentiments \n                    DROP CONSTRAINT IF EXISTS comment_sentiments_comment_id_fkey\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    ALTER TABLE comment_sentiments \n                    ADD CONSTRAINT comment_sentiments_comment_id_fkey \n                    FOREIGN KEY (comment_id) REFERENCES social_comments(id) ON DELETE CASCADE\n                \"\"\"))\n                \n                session.commit()\n                logger.info(\"Foreign keys actualizados\")\n                self.migration_history.append(\"Foreign keys actualizados\")\n                \n            except Exception as e:\n                session.rollback()\n                logger.error(f\"Error actualizando foreign keys: {e}\")\n                raise\n    \n    def _add_indexes(self):\n        \"\"\"Agrega índices para mejorar performance\"\"\"\n        logger.info(\"Agregando índices...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Índices para posts\n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_posts_platform \n                    ON social_posts(platform)\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_posts_category \n                    ON social_posts(category)\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_posts_post_time \n                    ON social_posts(post_time)\n                \"\"\"))\n                \n                # Índices para comentarios\n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_comments_platform \n                    ON social_comments(platform)\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_comments_category \n                    ON social_comments(category)\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_comments_comment_time \n                    ON social_comments(comment_time)\n                \"\"\"))\n                \n                # Índice compuesto para análisis por plataforma y fecha\n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_posts_platform_time \n                    ON social_posts(platform, post_time)\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    CREATE INDEX IF NOT EXISTS idx_social_comments_platform_time \n                    ON social_comments(platform, comment_time)\n                \"\"\"))\n                \n                session.commit()\n                logger.info(\"Índices agregados exitosamente\")\n                self.migration_history.append(\"Índices agregados\")\n                \n            except Exception as e:\n                session.rollback()\n                logger.error(f\"Error agregando índices: {e}\")\n                raise\n    \n    def _verify_migration(self):\n        \"\"\"Verifica que la migración se haya completado correctamente\"\"\"\n        logger.info(\"Verificando migración...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Verificar que las tablas existen\n                result = session.execute(text(\"\"\"\n                    SELECT table_name FROM information_schema.tables \n                    WHERE table_schema = 'public' \n                    AND table_name IN ('social_posts', 'social_comments')\n                \"\"\")).fetchall()\n                \n                table_names = [row[0] for row in result]\n                \n                if 'social_posts' not in table_names:\n                    raise Exception(\"Tabla social_posts no encontrada\")\n                \n                if 'social_comments' not in table_names:\n                    raise Exception(\"Tabla social_comments no encontrada\")\n                \n                # Verificar que los nuevos campos existen\n                posts_columns = session.execute(text(\"\"\"\n                    SELECT column_name FROM information_schema.columns \n                    WHERE table_name = 'social_posts'\n                \"\"\")).fetchall()\n                \n                posts_column_names = [row[0] for row in posts_columns]\n                required_posts_fields = ['platform', 'category', 'author_name', 'hashtags']\n                \n                for field in required_posts_fields:\n                    if field not in posts_column_names:\n                        raise Exception(f\"Campo {field} no encontrado en social_posts\")\n                \n                comments_columns = session.execute(text(\"\"\"\n                    SELECT column_name FROM information_schema.columns \n                    WHERE table_name = 'social_comments'\n                \"\"\")).fetchall()\n                \n                comments_column_names = [row[0] for row in comments_columns]\n                required_comments_fields = ['platform', 'category']\n                \n                for field in required_comments_fields:\n                    if field not in comments_column_names:\n                        raise Exception(f\"Campo {field} no encontrado en social_comments\")\n                \n                # Contar registros migrados\n                posts_count = session.execute(text(\"SELECT COUNT(*) FROM social_posts\")).scalar()\n                comments_count = session.execute(text(\"SELECT COUNT(*) FROM social_comments\")).scalar()\n                \n                logger.info(f\"Verificación exitosa: {posts_count} posts, {comments_count} comentarios\")\n                self.migration_history.append(f\"Verificación: {posts_count} posts, {comments_count} comentarios\")\n                \n            except Exception as e:\n                logger.error(f\"Error en verificación: {e}\")\n                raise\n    \n    def _rollback_migration(self):\n        \"\"\"Realiza rollback en caso de error\"\"\"\n        logger.warning(\"Realizando rollback de la migración...\")\n        \n        with self.db_manager.get_session() as session:\n            try:\n                # Restaurar desde backup si existe\n                session.execute(text(\"\"\"\n                    DROP TABLE IF EXISTS social_posts CASCADE\n                \"\"\"))\n                \n                session.execute(text(\"\"\"\n                    DROP TABLE IF EXISTS social_comments CASCADE\n                \"\"\"))\n                \n                # Restaurar backup si existe\n                if self._table_exists(session, 'fb_posts_backup'):\n                    session.execute(text(\"\"\"\n                        CREATE TABLE fb_posts AS SELECT * FROM fb_posts_backup\n                    \"\"\"))\n                \n                if self._table_exists(session, 'fb_comments_backup'):\n                    session.execute(text(\"\"\"\n                        CREATE TABLE fb_comments AS SELECT * FROM fb_comments_backup\n                    \"\"\"))\n                \n                session.commit()\n                logger.info(\"Rollback completado\")\n                \n            except Exception as e:\n                logger.error(f\"Error durante rollback: {e}\")\n    \n    def _table_exists(self, session, table_name):\n        \"\"\"Verifica si una tabla existe\"\"\"\n        result = session.execute(text(\"\"\"\n            SELECT EXISTS (\n                SELECT FROM information_schema.tables \n                WHERE table_schema = 'public' AND table_name = :table_name\n            )\n        \"\"\"), {\"table_name\": table_name}).scalar()\n        \n        return result\n    \n    def _print_migration_summary(self):\n        \"\"\"Imprime resumen de la migración\"\"\"\n        logger.info(\"=== RESUMEN DE MIGRACIÓN ===\")\n        for step in self.migration_history:\n            logger.info(f\"✅ {step}\")\n        \n        logger.info(\"\")\n        logger.info(\"Cambios realizados:\")\n        logger.info(\"• Tabla fb_posts → social_posts\")\n        logger.info(\"• Tabla fb_comments → social_comments\")\n        logger.info(\"• Agregados campos: platform, category, author_name, hashtags\")\n        logger.info(\"• Foreign keys actualizados\")\n        logger.info(\"• Índices optimizados agregados\")\n        logger.info(\"\")\n        logger.info(\"La base de datos ahora soporta Facebook y TikTok!\")\n\n\ndef main():\n    \"\"\"Función principal para ejecutar la migración\"\"\"\n    print(\"=== MIGRACIÓN DE BASE DE DATOS PARA SOPORTE MULTIPLATAFORMA ===\")\n    print(\"\")\n    print(\"Esta migración actualizará la base de datos para soportar:\")\n    print(\"• Facebook (existente)\")\n    print(\"• TikTok (nuevo)\")\n    print(\"• Campos adicionales para categorización\")\n    print(\"\")\n    \n    confirm = input(\"¿Deseas continuar? (s/N): \").strip().lower()\n    \n    if confirm not in ['s', 'si', 'y', 'yes']:\n        print(\"Migración cancelada por el usuario\")\n        return\n    \n    try:\n        migrator = DatabaseMigrator()\n        migrator.run_migration()\n        \n        print(\"\")\n        print(\"🎉 ¡MIGRACIÓN COMPLETADA EXITOSAMENTE!\")\n        print(\"\")\n        print(\"Próximos pasos:\")\n        print(\"1. Configurar token de Apify para TikTok\")\n        print(\"2. Ejecutar el collector multiplataforma\")\n        print(\"3. Analizar sentimientos con Hugging Face\")\n        \n    except Exception as e:\n        print(f\"\")\n        print(f\"❌ Error durante la migración: {e}\")\n        print(\"Por favor revisa los logs para más detalles\")\n        return 1\n\n\nif __name__ == \"__main__\":\n    main()"